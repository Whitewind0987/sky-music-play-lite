# UI Foundation

This directory contains gradual Tailwind, Radix UI, and lucide-react building blocks.

Do not migrate the existing UI all at once or use these components to change app behavior. Radix interactive components and lucide icon migration will be handled in later stages.
