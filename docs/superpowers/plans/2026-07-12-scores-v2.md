# scores-v2 长音支持实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让曲谱能表达每个音符的按住时长(v2 格式),并让预览/前台/后台三条播放路径按时值执行。

**Architecture:** v2 是 v1 的超集:歌曲级 `formatVersion: 2` 标记 + 音符级可选 `duration`(毫秒)。解析层单一路径按版本校验;播放计划事件从 `{timeMs, keys: string[]}` 升级为 `{timeMs, keys: [{key, holdMs?}]}`;Rust 引擎按键计算 keyup 截止时间(`holdMs/speed`,无则回退全局 `key_hold_ms`);延音尾巴统一定义为源时间的 `max(0, max(note.time+duration) − 最后一组 time)`,仅随倍速缩放,四处时长公式(Rust 时间线、预览调度器、元数据、内置索引)一致。

**Tech Stack:** TypeScript + React 19 + Vitest / Rust (Tauri v2, windows-sys)

**设计文档:** `docs/superpowers/specs/2026-07-12-scores-v2-design.md`

## Global Constraints(来自 AGENTS.md 与设计文档)

- 工作分支 `feature/scores-v2`,不碰 `main`;只本地提交,不推送。
- 每阶段收尾必须跑:`npm run test`、`npx tsc --noEmit`、`npm run build`、`cd src-tauri && cargo check && cargo test`。
- 播放行为改动必须同时覆盖预览/前台/后台三条路径;前台与后台共用 Rust worker,协议改动即覆盖两者。
- `duration` 约束:有限正数,`0 < duration <= 60000`(常量 `MAX_NOTE_DURATION_MS = 60000`)。
- `formatVersion` 约束:缺省视为 1;若存在必须是数字字面量 `1` 或 `2`;其他 → `formatVersionInvalid`。
- v1 歌曲中的 `duration` 字段一律忽略(不进入内部 Note),现有行为零变化。
- 固定 `key_hold_ms`(点按)不随倍速缩放(现状);`holdMs`(长音)随倍速缩放。
- 不新增依赖;新持久化字段必须有迁移/清洗 Vitest 覆盖;内置索引生成器与运行时解析规则必须一致。
- 提交信息风格:历史仓库风格(英文祈使句,如 "Add scores-v2 note duration parsing"),结尾带 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`。

---

## Stage 1:格式与解析

### Task 1: Note 类型 + v2 解析校验

**Files:**
- Modify: `src/types/score.ts`
- Modify: `src/lib/scoreFileImport.ts`
- Test: `src/lib/scoreFileImport.test.ts`(追加用例)

**Interfaces:**
- Produces: `Note.duration?: number`(毫秒,可选);`MAX_NOTE_DURATION_MS = 60000` 导出常量;新错误码 `"formatVersionInvalid" | "noteDurationInvalid"` 加入 `ScoreFileImportErrorCode`。
- 后续任务依赖:`parseScoreFileContent` / `parseScoreFileSongAtIndex` 返回的 `Song.songNotes[i].duration` 在 v2 歌曲上存在。

- [ ] **Step 1: 写失败测试**(追加到 `src/lib/scoreFileImport.test.ts`;先读该文件了解现有测试风格与工具函数,复用其构造方式)

```ts
describe("scores-v2 duration support", () => {
  const v2Song = (extra: Record<string, unknown> = {}, notes?: unknown[]) => ({
    name: "V2 Song",
    bpm: 120,
    bitsPerPage: 16,
    pitchLevel: 0,
    isComposed: true,
    formatVersion: 2,
    songNotes: notes ?? [
      { time: 0, key: "1Key0" },
      { time: 500, key: "1Key2", duration: 1500 },
    ],
    ...extra,
  });

  it("parses v2 songs and keeps note durations", () => {
    const songs = parseScoreFileContent(JSON.stringify([v2Song()]));

    expect(songs[0].songNotes[0].duration).toBeUndefined();
    expect(songs[0].songNotes[1].duration).toBe(1500);
  });

  it("ignores duration on v1 songs", () => {
    const song = { ...v2Song(), formatVersion: undefined };
    delete (song as Record<string, unknown>).formatVersion;
    const songs = parseScoreFileContent(JSON.stringify([song]));

    expect(songs[0].songNotes[1].duration).toBeUndefined();
  });

  it("accepts formatVersion 1 as v1", () => {
    const songs = parseScoreFileContent(
      JSON.stringify([{ ...v2Song(), formatVersion: 1 }]),
    );

    expect(songs[0].songNotes[1].duration).toBeUndefined();
  });

  it.each([[3], ["2"], [true], [null], [2.5]])(
    "rejects invalid formatVersion %p",
    (formatVersion) => {
      expect(() =>
        parseScoreFileContent(JSON.stringify([{ ...v2Song(), formatVersion }])),
      ).toThrowError(
        expect.objectContaining({ code: "formatVersionInvalid" }),
      );
    },
  );

  it.each([[0], [-1], ["1500"], [Number.NaN], [60001], [true]])(
    "rejects invalid v2 duration %p",
    (duration) => {
      expect(() =>
        parseScoreFileContent(
          JSON.stringify([
            v2Song({}, [{ time: 0, key: "1Key0", duration }]),
          ]),
        ),
      ).toThrowError(
        expect.objectContaining({ code: "noteDurationInvalid" }),
      );
    },
  );

  it("accepts duration at the 60000 cap", () => {
    const songs = parseScoreFileContent(
      JSON.stringify([v2Song({}, [{ time: 0, key: "1Key0", duration: 60000 }])]),
    );

    expect(songs[0].songNotes[0].duration).toBe(60000);
  });

  it("keeps durations absolute under isRelativeTime", () => {
    const songs = parseScoreFileContent(
      JSON.stringify([
        v2Song({ isRelativeTime: true }, [
          { time: 0, key: "1Key0", duration: 1200 },
          { time: 250, key: "1Key1", duration: 800 },
        ]),
      ]),
    );

    expect(songs[0].songNotes[1].time).toBe(250);
    expect(songs[0].songNotes[1].duration).toBe(800);
  });

  it("parses v2 song at index via parseScoreFileSongAtIndex", () => {
    const content = JSON.stringify([v2Song()]);
    const song = parseScoreFileSongAtIndex(content, 0);

    expect(song?.songNotes[1].duration).toBe(1500);
  });
});
```

- [ ] **Step 2: 跑测试确认失败** — `npm run test -- scoreFileImport`,期望新用例 FAIL(duration 未保留 / 错误码不存在)。

- [ ] **Step 3: 最小实现**

`src/types/score.ts`:

```ts
export type Note = {
  time: number;
  key: string;
  duration?: number;
};
```

`src/lib/scoreFileImport.ts` 改动点:

```ts
export const MAX_NOTE_DURATION_MS = 60000;

export type ScoreFileImportErrorCode =
  | "emptyFile"
  // ...现有成员保持不动...
  | "noteKeyInvalid"
  | "formatVersionInvalid"
  | "noteDurationInvalid";
```

`validateSong` 内(在 `resolveSongNotes` 之前读版本,传入 `validateNote`):

```ts
const formatVersion = readFormatVersion(value, songIndex);
// ...
const validatedNotes = songNotes.map((note, noteIndex) =>
  validateNote(note, name, noteIndex, formatVersion === 2),
);
```

新增 `readFormatVersion`:

```ts
function readFormatVersion(
  value: Record<string, unknown>,
  songIndex: number,
): 1 | 2 {
  const fieldValue = value.formatVersion;

  if (fieldValue === undefined) {
    return 1;
  }

  if (fieldValue === 1 || fieldValue === 2) {
    return fieldValue;
  }

  throw new ScoreFileImportError("formatVersionInvalid", { songIndex });
}
```

`validateNote` 增加第四参数(v1 路径不读取 `duration`,行为不变):

```ts
function validateNote(
  value: unknown,
  songName: string,
  noteIndex: number,
  allowDuration: boolean,
): Note {
  if (!isRecord(value)) {
    throw new ScoreFileImportError("noteNotObject", { noteIndex, songName });
  }

  const time = readFlexibleNoteTime(value.time, songName, noteIndex);
  const key = value.key;

  if (typeof key !== "string") {
    throw new ScoreFileImportError("noteKeyInvalid", { noteIndex, songName });
  }

  if (!allowDuration || value.duration === undefined) {
    return { time, key };
  }

  const duration = value.duration;

  if (
    typeof duration !== "number" ||
    !Number.isFinite(duration) ||
    duration <= 0 ||
    duration > MAX_NOTE_DURATION_MS
  ) {
    throw new ScoreFileImportError("noteDurationInvalid", {
      noteIndex,
      songName,
    });
  }

  return { time, key, duration };
}
```

`normalizeRelativeSongNotes` 已用 `{ ...note, time }` 展开,duration 自动保留,无需改动(测试覆盖确认)。

- [ ] **Step 4: 跑测试确认通过** — `npm run test -- scoreFileImport`,全绿(含既有回归)。

- [ ] **Step 5: 提交** — `git add src/types/score.ts src/lib/scoreFileImport.ts src/lib/scoreFileImport.test.ts && git commit -m "Add scores-v2 note duration parsing"`(带 Co-Authored-By 行)。

### Task 2: 导入错误文案(中英)

**Files:**
- Modify: `src/i18n/uiText.ts`(两处 `importErrors` 块,约 239 行与 782 行)

**Interfaces:**
- Consumes: Task 1 的错误码。`formatImportError`(`src/lib/importErrors.ts`)按 `text.score.importErrors[error.code]` 取文案,新增键后类型自动约束两语言齐全。

- [ ] **Step 1: 补文案**

中文块(`importErrors` 内追加):

```ts
formatVersionInvalid:
  "第 {songIndex} 首曲谱的 formatVersion 无效。仅支持数字 1 或 2。",
noteDurationInvalid:
  "曲谱“{songName}”里的第 {noteIndex} 个音符 duration 无效。必须是 0 到 60000 之间的正数毫秒。",
```

英文块:

```ts
formatVersionInvalid:
  "Song at index {songIndex} has an invalid formatVersion. Only the numbers 1 and 2 are supported.",
noteDurationInvalid:
  'Note {noteIndex} in song "{songName}" has an invalid duration. It must be a positive number of milliseconds no greater than 60000.',
```

- [ ] **Step 2: 验证** — `npx tsc --noEmit` 通过(若 UiText 类型要求键齐全,缺一边会报错);`npm run test` 全绿。

- [ ] **Step 3: 提交** — `git commit -m "Add scores-v2 import error messages"`。

### Stage 1 收尾

- [ ] `npm run test` / `npx tsc --noEmit` / `npm run build` / `cd src-tauri && cargo check`(Rust 未动,应直接绿)。

---

## Stage 2:存储与元数据

### Task 3: 指纹与 sustainTailMs 元数据

**Files:**
- Modify: `src/types/library.ts`(`LocalSongMetadata`)
- Modify: `src/lib/libraryCollections.ts`(`getSongFingerprint`、`createLocalSongMetadata`、`getLibrarySongRawDurationMs`)
- Test: `src/lib/libraryCollections.test.ts`(追加)

**Interfaces:**
- Produces: `LocalSongMetadata.sustainTailMs?: number`(仅当 > 0 时写入);`getSustainTailMs(notes: Note[]): number` 导出(供 Task 9/11 复用,定义:`max(0, max(note.time + note.duration) − 最后一组 time)`,无时值音符贡献 0)。
- 指纹规则:带 `duration` 的音符哈希元组为 `[time, key, duration]`,否则 `[time, key]` — 纯 v1 歌曲指纹与历史输出逐字节一致。

- [ ] **Step 1: 写失败测试**(追加到 `libraryCollections.test.ts`,先读现有测试构造方式)

```ts
describe("scores-v2 sustain metadata", () => {
  const baseSong = (notes: Note[]): Song => ({
    name: "Sustain Song",
    bpm: 120,
    bitsPerPage: 16,
    pitchLevel: 0,
    isComposed: true,
    songNotes: notes,
  });

  it("computes sustainTailMs from the longest tail past the last group", () => {
    const metadata = createLocalSongMetadata(
      baseSong([
        { time: 0, key: "1Key0", duration: 5000 },
        { time: 1000, key: "1Key1", duration: 500 },
      ]),
    );

    expect(metadata.sustainTailMs).toBe(4000);
    expect(metadata.lastNoteTimeMs).toBe(1000);
  });

  it("omits sustainTailMs when no duration extends past the last group", () => {
    const metadata = createLocalSongMetadata(
      baseSong([
        { time: 0, key: "1Key0" },
        { time: 1000, key: "1Key1", duration: 200 },
      ]),
    );

    expect(metadata.sustainTailMs).toBe(200);
  });

  it("keeps v1 fingerprints unchanged and differentiates v2 durations", () => {
    const v1Notes: Note[] = [{ time: 0, key: "1Key0" }];
    const legacyFingerprint = getSongFingerprint(baseSong(v1Notes));
    const withDuration = getSongFingerprint(
      baseSong([{ time: 0, key: "1Key0", duration: 800 }]),
    );

    expect(withDuration).not.toBe(legacyFingerprint);
    expect(getSongFingerprint(baseSong(v1Notes))).toBe(legacyFingerprint);
  });

  it("includes sustain tail in raw duration for local imports", () => {
    const librarySong = createLibrarySong(
      baseSong([{ time: 1000, key: "1Key0", duration: 2500 }]),
    );

    expect(getLibrarySongRawDurationMs(librarySong)).toBe(3500);
  });
});
```

(注意第二个用例:`time 1000, duration 200` 的结束是 1200,尾巴 = 200 > 0,所以字段存在;真正"无尾巴"的是全点按曲——补一个断言 `createLocalSongMetadata(baseSong([{ time: 0, key: "1Key0" }])).sustainTailMs` 为 `undefined`。)

- [ ] **Step 2: 确认失败** — `npm run test -- libraryCollections`。

- [ ] **Step 3: 实现**

`src/types/library.ts`:在 `noteGroupDelaysMs?: number[];` 后加 `sustainTailMs?: number;`。

`src/lib/libraryCollections.ts`:

```ts
export function getSustainTailMs(notes: Note[]): number {
  const lastGroupTimeMs = notes.reduce(
    (lastTime, note) =>
      Number.isFinite(note.time) ? Math.max(lastTime, note.time) : lastTime,
    0,
  );
  const maxNoteEndMs = notes.reduce((maxEnd, note) => {
    if (!Number.isFinite(note.time) || note.duration === undefined) {
      return maxEnd;
    }

    return Math.max(maxEnd, note.time + note.duration);
  }, 0);

  return Math.max(0, maxNoteEndMs - lastGroupTimeMs);
}
```

`createLocalSongMetadata` 返回对象追加(与 `noteGroupDelaysMs` 同样的可选写法):

```ts
const sustainTailMs = getSustainTailMs(song.songNotes);
// ...
  ...(sustainTailMs > 0 ? { sustainTailMs } : {}),
```

`getSongFingerprint` 的 notes 映射改为:

```ts
notes: song.songNotes.map((note) =>
  note.duration === undefined
    ? [note.time, note.key]
    : [note.time, note.key, note.duration],
),
```

`getLibrarySongRawDurationMs` local-import 分支改为:

```ts
return (
  librarySong.metadata.lastNoteTimeMs +
  (librarySong.metadata.sustainTailMs ?? 0)
);
```

- [ ] **Step 4: 确认通过** — `npm run test -- libraryCollections`,并全量 `npm run test`(留意依赖指纹/时长的既有用例)。

- [ ] **Step 5: 提交** — `git commit -m "Add sustain tail metadata and fingerprint coverage"`。

### Task 4: appData 清洗器支持 sustainTailMs

**Files:**
- Modify: `src/lib/appData.ts`(`sanitizeLocalSongMetadata`,约 540-591 行)
- Test: `src/lib/appData.test.ts`(追加)

**Interfaces:**
- Consumes: `LocalSongMetadata.sustainTailMs?: number`。
- 规则:字段缺失 → 通过(不写入);存在但非有限数或 < 0 → 整条元数据判无效(返回 null,与 `noteGroupDelaysMs` 同策略);合法 → 原样保留。

- [ ] **Step 1: 写失败测试**(追加到 `appData.test.ts`,复用现有的合法元数据构造函数)

```ts
it("keeps valid sustainTailMs in persisted local metadata", () => {
  // 用现有测试的合法 rawMetadata 构造,追加 sustainTailMs: 1200
  // 断言恢复结果 metadata.sustainTailMs === 1200
});

it("rejects local metadata with invalid sustainTailMs", () => {
  // sustainTailMs: -1 与 sustainTailMs: "x" 两种,断言该歌曲被丢弃(与其他无效元数据用例一致)
});

it("restores metadata without sustainTailMs unchanged", () => {
  // 不含该字段,断言 metadata.sustainTailMs === undefined
});
```

(具体构造以 `appData.test.ts` 现有 helper 为准——先读文件,模仿最接近的 `noteGroupDelaysMs` 用例写法,写出完整断言。)

- [ ] **Step 2: 确认失败** — `npm run test -- appData`。

- [ ] **Step 3: 实现** — `sanitizeLocalSongMetadata` 在 `noteGroupDelaysMs` 处理后追加:

```ts
const rawSustainTailMs = rawMetadata.sustainTailMs;

if (
  rawSustainTailMs !== undefined &&
  (typeof rawSustainTailMs !== "number" ||
    !Number.isFinite(rawSustainTailMs) ||
    rawSustainTailMs < 0)
) {
  return null;
}
```

返回对象追加:

```ts
...(typeof rawSustainTailMs === "number" && rawSustainTailMs > 0
  ? { sustainTailMs: rawSustainTailMs }
  : {}),
```

- [ ] **Step 4: 确认通过** — `npm run test -- appData`。

- [ ] **Step 5: 提交** — `git commit -m "Sanitize persisted sustain tail metadata"`。

### Task 5: Rust 导入存储的时值感知

**Files:**
- Modify: `src-tauri/src/imported_scores.rs`(`SemanticNote` 约 87-91 行;托管音符校验约 717-745 行;`#[cfg(test)]` 模块追加)

**Interfaces:**
- `SemanticNote` 增加 `duration: Option<f64>`(serde 对 Option 缺省 None,旧文件兼容);语义比较(derive PartialEq)自动覆盖时值。
- 托管文件校验:音符存在 `duration` 字段时必须是有限正数,否则报错(信息风格与 time/key 一致)。

- [ ] **Step 1: 写失败测试**(追加到 `imported_scores.rs` tests 模块,模仿 `managed_song_validation_rejects_invalid_note_time_or_key` 的写法)

```rust
#[test]
fn managed_song_validation_rejects_invalid_note_duration() {
    let test_dir = unique_test_dir("validate_note_duration");
    // 写入 songNotes: [{"time":0,"key":"1Key0","duration":-5}] 的文件
    // 断言错误包含 "songNotes[0].duration must be a positive number"
}

#[test]
fn semantic_comparison_detects_duration_changes() {
    // 两个仅 duration 不同的 song Value,断言 semantic_song_from_value 结果不相等
}

#[test]
fn managed_song_validation_accepts_v2_duration() {
    // {"time":0,"key":"1Key0","duration":1500} 校验通过
}
```

- [ ] **Step 2: 确认失败** — `cd src-tauri && cargo test imported_scores`。

- [ ] **Step 3: 实现**

```rust
#[derive(Debug, Deserialize, PartialEq)]
struct SemanticNote {
    key: String,
    time: f64,
    duration: Option<f64>,
}
```

校验循环(在 key 检查后追加):

```rust
if let Some(duration) = note_object.get("duration") {
    let is_valid_duration = duration
        .as_f64()
        .is_some_and(|value| value.is_finite() && value > 0.0);

    if !is_valid_duration {
        return Err(format!(
            "Imported score file at {} songNotes[{}].duration must be a positive number",
            display_path(file_path),
            note_index
        ));
    }
}
```

(错误信息前缀以该函数现有写法为准,保持同款格式化。)

- [ ] **Step 4: 确认通过** — `cargo test imported_scores`,并跑全量 `cargo test`。

- [ ] **Step 5: 提交** — `git commit -m "Verify imported score durations in Rust storage"`。

### Stage 2 收尾

- [ ] 全量门禁:`npm run test` / `npx tsc --noEmit` / `npm run build` / `cd src-tauri && cargo check && cargo test`。
- [ ] 检查 `src-tauri/Cargo.toml` 无行尾噪声。

---

## Stage 3:计划协议与 Rust 引擎

### Task 6: 前端播放计划携带 holdMs

**Files:**
- Modify: `src/lib/scoreKeyMapping.ts`
- Modify: `src/lib/tauriApi.ts`(`BackgroundPlaybackPlanEvent`,约 56-59 行)
- Modify: `src/hooks/usePlaybackPlanPreparation.ts`(`buildPlaybackPlan`,约 107-115 行)
- Test: 新建 `src/lib/scoreKeyMapping.test.ts`

**Interfaces:**
- Produces:
  ```ts
  export type MappedPlaybackKey = { key: string; holdMs?: number };
  export function prepareMappedKeyboardKeyGroups(
    notes: Note[], keyMapping: KeyMapping,
  ): Map<number, MappedPlaybackKey[]>;
  // tauriApi:
  export type PlannedPlaybackKey = { key: string; holdMs?: number };
  export type BackgroundPlaybackPlanEvent = { keys: PlannedPlaybackKey[]; timeMs: number };
  ```
- 去重规则:同一 time 同一映射键出现多次 → 只保留一个条目,`holdMs` 取各次出现的最大值;任一出现无 `holdMs`(点按)时,已有 `holdMs` 的出现胜出(点按视为最小)。

- [ ] **Step 1: 写失败测试**(`src/lib/scoreKeyMapping.test.ts` 新文件)

```ts
import { describe, expect, it } from "vitest";
import { prepareMappedKeyboardKeyGroups } from "./scoreKeyMapping";
import { defaultKeyMapping } from "../types/keyMapping";

describe("prepareMappedKeyboardKeyGroups", () => {
  it("carries note durations as holdMs", () => {
    const groups = prepareMappedKeyboardKeyGroups(
      [
        { time: 0, key: "1Key0", duration: 1500 },
        { time: 0, key: "1Key1" },
      ],
      defaultKeyMapping,
    );

    expect(groups.get(0)).toEqual([
      { key: "y", holdMs: 1500 },
      { key: "u" },
    ]);
  });

  it("dedupes same key at same time keeping the longest hold", () => {
    const groups = prepareMappedKeyboardKeyGroups(
      [
        { time: 0, key: "1Key0", duration: 500 },
        { time: 0, key: "1Key0", duration: 1500 },
        { time: 0, key: "1Key0" },
      ],
      defaultKeyMapping,
    );

    expect(groups.get(0)).toEqual([{ key: "y", holdMs: 1500 }]);
  });

  it("keeps tap entries deduped without holdMs", () => {
    const groups = prepareMappedKeyboardKeyGroups(
      [
        { time: 0, key: "1Key0" },
        { time: 0, key: "1Key0" },
      ],
      defaultKeyMapping,
    );

    expect(groups.get(0)).toEqual([{ key: "y" }]);
  });
});
```

- [ ] **Step 2: 确认失败** — `npm run test -- scoreKeyMapping`。

- [ ] **Step 3: 实现**

`src/lib/scoreKeyMapping.ts`:

```ts
export type MappedPlaybackKey = {
  key: string;
  holdMs?: number;
};

export function prepareMappedKeyboardKeyGroups(
  notes: Note[],
  keyMapping: KeyMapping,
): Map<number, MappedPlaybackKey[]> {
  const mappedGroups = new Map<number, MappedPlaybackKey[]>();

  for (const note of notes) {
    const mappedKey = mapScoreNoteToKeyboardKey(note, keyMapping);
    const currentGroup = mappedGroups.get(note.time) ?? [];
    const existingEntry = currentGroup.find((entry) => entry.key === mappedKey);

    if (existingEntry) {
      if (
        note.duration !== undefined &&
        (existingEntry.holdMs === undefined || note.duration > existingEntry.holdMs)
      ) {
        existingEntry.holdMs = note.duration;
      }
    } else {
      currentGroup.push(
        note.duration === undefined
          ? { key: mappedKey }
          : { key: mappedKey, holdMs: note.duration },
      );
    }

    mappedGroups.set(note.time, currentGroup);
  }

  return mappedGroups;
}
```

`src/lib/tauriApi.ts`:

```ts
export type PlannedPlaybackKey = {
  holdMs?: number;
  key: string;
};

export type BackgroundPlaybackPlanEvent = {
  keys: PlannedPlaybackKey[];
  timeMs: number;
};
```

`usePlaybackPlanPreparation.ts` 的 `buildPlaybackPlan` 结构不变(分组结果直接透传即可编译)。

- [ ] **Step 4: 确认通过 + 编译** — `npm run test -- scoreKeyMapping` 与 `npx tsc --noEmit`(此刻 Rust 侧未同步,前端必须已编译通过;若其他前端引用点报类型错,一并修复——搜索 `BackgroundPlaybackPlanEvent` 与 `prepareMappedKeyboardKeyGroups` 的全部使用处)。

- [ ] **Step 5: 提交** — `git commit -m "Carry per-key hold durations in playback plans"`。

### Task 7: Rust 引擎按键时值调度

**Files:**
- Modify: `src-tauri/src/experimental_input/background_playback.rs`:
  - `BackgroundPlaybackPlanEvent`(16-21 行)、`TimelineGroup`(112-117 行)、`PlaybackTimeline` 构建(1175-1218 行)、`build_source_groups`(1128-1161 行)、`unique_timeline_keys`(1352-1365 行)、`play_group`(894-935 行)、`next_deadline_ms` 不变、tests 模块 fixtures
- Test: 同文件 `#[cfg(test)]`(既有 fixture 更新 + 新用例)

**Interfaces:**
- Produces:
  ```rust
  #[derive(Debug, Clone, Deserialize, PartialEq)]
  #[serde(rename_all = "camelCase")]
  pub struct PlannedKey { pub key: String, pub hold_ms: Option<f64> }
  pub struct BackgroundPlaybackPlanEvent { pub time_ms: f64, pub keys: Vec<PlannedKey> }
  struct TimelineGroup { source_time_ms: f64, adjusted_start_ms: f64, keys: Arc<[PlannedKey]> }
  ```
- keyup 截止:`hold_ms.map(|h| h / options.playback_speed).unwrap_or(key_hold_ms)`。
- 时间线:`tail_source_ms = max(0, max_i(source_time_i + max_hold_i) − last_source_time)`;`total_ms = last_adjusted_start + tail_source_ms / speed`;`finish_ms = last_adjusted_start + max(tail_source_ms, NOTE_HIGHLIGHT_MS) / speed`。

- [ ] **Step 1: 更新既有测试 fixture 并新增失败测试**

既有 `plan()` fixture 键改为:

```rust
fn planned_key(key: &str) -> PlannedKey {
    PlannedKey { key: key.to_string(), hold_ms: None }
}

fn held_key(key: &str, hold_ms: f64) -> PlannedKey {
    PlannedKey { key: key.to_string(), hold_ms: Some(hold_ms) }
}
```

新增用例:

```rust
#[test]
fn build_source_groups_dedupes_same_key_keeping_longest_hold() {
    let groups = build_source_groups(&[
        BackgroundPlaybackPlanEvent {
            time_ms: 0.0,
            keys: vec![held_key("y", 500.0), planned_key("y"), held_key("y", 1500.0)],
        },
    ])
    .unwrap();

    assert_eq!(groups[0].keys.len(), 1);
    assert_eq!(groups[0].keys[0].hold_ms, Some(1500.0));
}

#[test]
fn build_source_groups_rejects_invalid_hold() {
    for hold in [0.0, -1.0, f64::NAN, f64::INFINITY] {
        let result = build_source_groups(&[BackgroundPlaybackPlanEvent {
            time_ms: 0.0,
            keys: vec![held_key("y", hold)],
        }]);
        assert!(result.is_err());
    }
}

#[test]
fn timeline_total_includes_scaled_sustain_tail() {
    let groups = build_source_groups(&[
        BackgroundPlaybackPlanEvent { time_ms: 0.0, keys: vec![held_key("y", 5000.0)] },
        BackgroundPlaybackPlanEvent { time_ms: 1000.0, keys: vec![planned_key("u")] },
    ])
    .unwrap();
    let timeline = build_timeline_from_groups(&groups, &options(0.0, 2.0)).unwrap();

    // last adjusted start = 1000/2 = 500; tail source = 5000-1000 = 4000; scaled = 2000
    assert_eq!(timeline.total_ms, 2500.0);
    assert_eq!(timeline.finish_ms, 2500.0);
}

#[test]
fn timeline_without_holds_keeps_note_highlight_finish() {
    let groups = build_source_groups(&[BackgroundPlaybackPlanEvent {
        time_ms: 0.0,
        keys: vec![planned_key("y")],
    }])
    .unwrap();
    let timeline = build_timeline_from_groups(&groups, &options(0.0, 1.0)).unwrap();

    assert_eq!(timeline.total_ms, 0.0);
    assert_eq!(timeline.finish_ms, NOTE_HIGHLIGHT_MS);
}

#[test]
fn effective_hold_scales_with_playback_speed() {
    assert_eq!(effective_hold_ms(Some(1000.0), 2.0, 40.0), 500.0);
    assert_eq!(effective_hold_ms(None, 2.0, 40.0), 40.0);
}
```

(注意:`timeline_without_holds` 里 total_ms 语义变化——现值 = 最后组 start;tail 为 0 时保持现状,该用例与既有 total/finish 测试共同守住回归。既有测试若断言 `total_ms`/`finish_ms`,按新公式核对:无 hold 时结果与旧公式一致,不应需要改值。)

- [ ] **Step 2: 确认编译失败/测试失败** — `cargo test background_playback`。

- [ ] **Step 3: 实现**

结构体:

```rust
#[derive(Debug, Clone, Deserialize, PartialEq)]
#[serde(rename_all = "camelCase")]
pub struct PlannedKey {
    pub key: String,
    pub hold_ms: Option<f64>,
}

#[derive(Debug, Clone, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct BackgroundPlaybackPlanEvent {
    pub time_ms: f64,
    pub keys: Vec<PlannedKey>,
}

#[derive(Debug, Clone)]
struct TimelineGroup {
    source_time_ms: f64,
    adjusted_start_ms: f64,
    keys: Arc<[PlannedKey]>,
}
```

`build_source_groups`:合并同刻事件后,对每组做键去重取最大 hold,并校验 hold:

```rust
fn build_source_groups(plan: &[BackgroundPlaybackPlanEvent]) -> Result<Vec<TimelineGroup>, String> {
    let mut grouped_events = plan.to_vec();
    grouped_events.sort_by(|left, right| left.time_ms.total_cmp(&right.time_ms));

    let mut grouped_keys = Vec::<(f64, Vec<PlannedKey>)>::new();

    for event in grouped_events {
        if !event.time_ms.is_finite() {
            return Err("Background playback event time must be finite.".to_string());
        }

        if event.keys.is_empty() {
            return Err("Background playback event must contain at least one key.".to_string());
        }

        for key in &event.keys {
            if let Some(hold_ms) = key.hold_ms {
                if !hold_ms.is_finite() || hold_ms <= 0.0 {
                    return Err(
                        "Background playback key hold duration must be a positive number."
                            .to_string(),
                    );
                }
            }
        }

        if let Some((last_time_ms, last_keys)) = grouped_keys.last_mut() {
            if *last_time_ms == event.time_ms {
                last_keys.extend(event.keys);
                continue;
            }
        }

        grouped_keys.push((event.time_ms, event.keys));
    }

    Ok(grouped_keys
        .into_iter()
        .map(|(source_time_ms, keys)| TimelineGroup {
            source_time_ms,
            adjusted_start_ms: 0.0,
            keys: Arc::from(dedupe_planned_keys(keys)),
        })
        .collect())
}

fn dedupe_planned_keys(keys: Vec<PlannedKey>) -> Vec<PlannedKey> {
    let mut deduped = Vec::<PlannedKey>::new();

    for key in keys {
        if let Some(existing) = deduped.iter_mut().find(|entry| entry.key == key.key) {
            existing.hold_ms = match (existing.hold_ms, key.hold_ms) {
                (Some(left), Some(right)) => Some(left.max(right)),
                (None, Some(right)) => Some(right),
                (left, None) => left,
            };
        } else {
            deduped.push(key);
        }
    }

    deduped
}
```

`build_timeline_from_groups`:在现有累加循环里同步收集最大源结束时间:

```rust
let mut max_source_end_ms = f64::NEG_INFINITY;
// 循环内(previous_source_time_ms 更新之后):
let group_max_hold_ms = group
    .keys
    .iter()
    .filter_map(|key| key.hold_ms)
    .fold(0.0_f64, f64::max);
max_source_end_ms = max_source_end_ms.max(group.source_time_ms + group_max_hold_ms);
// 循环后:
let last_source_time_ms = groups.last().map(|g| g.source_time_ms).unwrap_or(0.0);
let last_adjusted_start_ms = adjusted_groups
    .last()
    .map(|group| group.adjusted_start_ms)
    .unwrap_or(0.0);
let tail_source_ms = (max_source_end_ms - last_source_time_ms).max(0.0);
let total_ms = last_adjusted_start_ms + tail_source_ms / options.playback_speed;
let finish_ms =
    last_adjusted_start_ms + tail_source_ms.max(NOTE_HIGHLIGHT_MS) / options.playback_speed;
```

`unique_timeline_keys` 迭代 `key.key.clone()`;`play_group` 改造:

```rust
fn play_group(&mut self, group: &TimelineGroup) -> Result<(), String> {
    let keys_to_release = group
        .keys
        .iter()
        .filter(|key| self.active_generations.contains_key(&key.key))
        .map(|key| key.key.clone())
        .collect::<Vec<_>>();

    if !keys_to_release.is_empty() {
        self.send_key_up_group(&keys_to_release)?;
        for key in keys_to_release {
            self.active_generations.remove(&key);
        }
    }

    let key_names = group
        .keys
        .iter()
        .map(|key| key.key.clone())
        .collect::<Vec<_>>();

    if let Err(error) = self.send_key_down_group(&key_names) {
        let _ = self.send_key_up_group(&key_names);
        return Err(error);
    }

    // ...logged_first_key_down 不变...

    for key in group.keys.iter() {
        let generation = self.next_generation;
        self.next_generation = self.next_generation.saturating_add(1).max(1);
        self.active_generations.insert(key.key.clone(), generation);
        self.scheduled_key_ups.push(ScheduledKeyUp {
            deadline_at: key_up_deadline_from_actual_send(
                Instant::now(),
                effective_hold_ms(key.hold_ms, self.options.playback_speed, self.key_hold_ms),
            ),
            generation,
            key: key.key.clone(),
        });
    }

    Ok(())
}
```

新增纯函数(放在 `key_up_deadline_from_actual_send` 旁):

```rust
fn effective_hold_ms(hold_ms: Option<f64>, playback_speed: f64, key_hold_ms: f64) -> f64 {
    match hold_ms {
        Some(hold) => hold / playback_speed,
        None => key_hold_ms,
    }
}
```

(`play_group` 原有的 `unique_keys` 调用删除——组内去重已在 `build_source_groups` 完成;`unique_keys` 若无其他调用点则一并删除。)

- [ ] **Step 4: 确认通过** — `cargo test` 全量 + `cargo check`。

- [ ] **Step 5: 前后端联合编译** — `npx tsc --noEmit` + `npm run build`(协议两侧 camelCase `holdMs` 对齐:serde `rename_all = "camelCase"` 将 `hold_ms` 映射为 `holdMs`,前端可选字段缺省为 undefined → serde None)。

- [ ] **Step 6: 提交** — `git commit -m "Schedule per-key hold durations in playback engine"`。

### Stage 3 收尾

- [ ] 全量门禁四连 + `Cargo.toml` 行尾检查。
- [ ] 冒烟:`npm run tauri dev` 手动验证 v1 曲目后台/前台播放无回归(点按行为、暂停/继续/seek/倍速)。

---

## Stage 4:预览路径

### Task 8: 预览调度器时长计入延音尾巴

**Files:**
- Modify: `src/lib/playbackScheduler.ts`
- Test: `src/lib/playbackScheduler.test.ts`(追加)

**Interfaces:**
- Consumes: `Note.duration`、`getSustainTailMs`(from `./libraryCollections` — 若引入造成循环依赖,把 `getSustainTailMs` 移到 `src/lib/sustainTail.ts` 并双向导入)。
- Produces:
  - `getAdjustedPreviewDurationMs(notes, options)`:返回值 = 原公式 + `getSustainTailMs(notes) / playbackSpeed`。
  - `getAdjustedPreviewDurationFromMetadata(metadata, options)`:参数 metadata 增加 `sustainTailMs`(`Pick` 联合中补充),返回值 += `(metadata.sustainTailMs ?? 0) / playbackSpeed`。
  - 调度器 finish:最后一组后的收尾延迟 = `max(NOTE_HIGHLIGHT_MS, tailSourceMs) / playbackSpeed`(替换现 `getScaledDelayMs(NOTE_HIGHLIGHT_MS, liveOptions)`),`updateTotalFromRemaining` 传入该值(替换现 `0`)。

- [ ] **Step 1: 写失败测试**(追加到 `playbackScheduler.test.ts`;先读现有测试,复用其 fake timer 模式)

```ts
describe("scores-v2 sustain tails", () => {
  it("includes scaled sustain tail in adjusted duration", () => {
    const durationMs = getAdjustedPreviewDurationMs(
      [
        { time: 0, key: "1Key0", duration: 5000 },
        { time: 1000, key: "1Key1" },
      ],
      { noteIntervalDelayMs: 0, playbackSpeed: 2 },
    );

    // base = 1000/2 = 500; tail = (5000-1000)/2 = 2000
    expect(durationMs).toBe(2500);
  });

  it("keeps v1 durations unchanged", () => {
    const notes = [
      { time: 0, key: "1Key0" },
      { time: 1000, key: "1Key1" },
    ];

    expect(
      getAdjustedPreviewDurationMs(notes, {
        noteIntervalDelayMs: 0,
        playbackSpeed: 1,
      }),
    ).toBe(1000);
  });

  it("includes sustainTailMs from metadata", () => {
    const durationMs = getAdjustedPreviewDurationFromMetadata(
      {
        lastNoteTimeMs: 1000,
        noteGroupCount: 2,
        noteGroupDelaysMs: [0, 1000],
        sustainTailMs: 4000,
      },
      { noteIntervalDelayMs: 0, playbackSpeed: 2 },
    );

    expect(durationMs).toBe(2500);
  });

  it("delays preview finish until the sustain tail ends", () => {
    // fake timers:两音符曲,第二个音符 duration 4000;
    // 断言 onFinish 在最后一组后 4000ms(1x 速度)才触发,而不是 300ms。
  });
});
```

- [ ] **Step 2: 确认失败** — `npm run test -- playbackScheduler`。

- [ ] **Step 3: 实现**(严格按 Interfaces 描述;`schedulePreviewPlayback` 内部预计算 `tailSourceMs = getSustainTailMs(sortedNotes)`,`playCurrentGroup` 的最后一组分支改为:

```ts
if (!nextGroup) {
  const finishDelayMs = getScaledDelayMs(
    Math.max(NOTE_HIGHLIGHT_MS, tailSourceMs),
    liveOptions,
  );
  updateTotalFromRemaining(finishDelayMs);
  scheduleTask("finish", finishDelayMs);
  return;
}
```

注意 `getAdjustedPreviewDurationMs` 同步加尾巴后,`totalMs` 初值与运行时 `updateTotalFromRemaining` 收敛到同一数值——两处公式必须一致,否则进度条会跳变;`reschedulePendingTaskAfterOptionsChange` 的 `scheduledTask === "finish"` 分支已按剩余比例重算,无需额外处理。)

- [ ] **Step 4: 确认通过** — `npm run test -- playbackScheduler` + 全量。

- [ ] **Step 5: 提交** — `git commit -m "Include sustain tails in preview durations"`。

### Task 9: 键盘预览按键级长音高亮

**Files:**
- Create: `src/lib/previewActiveKeys.ts`
- Create: `src/lib/previewActiveKeys.test.ts`
- Modify: `src/hooks/usePreviewPlayback.ts`(note group 回调约 184-194 行;pause/stop/finish 清空处 139/194/289 行)

**Interfaces:**
- Produces(纯函数,不依赖 React):
  ```ts
  export type PreviewActiveKeyEntry = { key: string; expiresAtMs: number | null };
  export function applyNoteGroupToPreviewActiveKeys(
    currentEntries: PreviewActiveKeyEntry[],
    notes: Note[],
    nowMs: number,
    playbackSpeed: number,
  ): PreviewActiveKeyEntry[];
  export function prunePreviewActiveKeys(
    currentEntries: PreviewActiveKeyEntry[], nowMs: number,
  ): PreviewActiveKeyEntry[];
  export function getNextPreviewExpiryMs(
    currentEntries: PreviewActiveKeyEntry[],
  ): number | null;
  ```
- 语义:`expiresAtMs === null` 为点按条目(被下一组整体替换,现行为);有到期时间的长音条目跨组保留直到过期;新组中出现同键时新条目接管旧条目。`activeKeys: string[]`(hook 对外接口)不变,由 entries 派生 `entries.map((e) => e.key)`。

- [ ] **Step 1: 写失败测试**(`previewActiveKeys.test.ts`,完整用例)

```ts
import { describe, expect, it } from "vitest";
import {
  applyNoteGroupToPreviewActiveKeys,
  getNextPreviewExpiryMs,
  prunePreviewActiveKeys,
} from "./previewActiveKeys";

describe("previewActiveKeys", () => {
  it("replaces tap entries with the next group", () => {
    const first = applyNoteGroupToPreviewActiveKeys(
      [],
      [{ time: 0, key: "1Key0" }],
      0,
      1,
    );
    const second = applyNoteGroupToPreviewActiveKeys(
      first,
      [{ time: 500, key: "1Key1" }],
      500,
      1,
    );

    expect(second).toEqual([{ key: "1Key1", expiresAtMs: null }]);
  });

  it("keeps held keys across groups until they expire", () => {
    const first = applyNoteGroupToPreviewActiveKeys(
      [],
      [{ time: 0, key: "1Key0", duration: 2000 }],
      0,
      1,
    );
    const second = applyNoteGroupToPreviewActiveKeys(
      first,
      [{ time: 500, key: "1Key1" }],
      500,
      1,
    );

    expect(second).toEqual([
      { key: "1Key0", expiresAtMs: 2000 },
      { key: "1Key1", expiresAtMs: null },
    ]);
  });

  it("scales hold expiry by playback speed", () => {
    const entries = applyNoteGroupToPreviewActiveKeys(
      [],
      [{ time: 0, key: "1Key0", duration: 2000 }],
      1000,
      2,
    );

    expect(entries).toEqual([{ key: "1Key0", expiresAtMs: 2000 }]);
  });

  it("lets a new press take over the same held key", () => {
    const first = applyNoteGroupToPreviewActiveKeys(
      [],
      [{ time: 0, key: "1Key0", duration: 5000 }],
      0,
      1,
    );
    const second = applyNoteGroupToPreviewActiveKeys(
      first,
      [{ time: 500, key: "1Key0" }],
      500,
      1,
    );

    expect(second).toEqual([{ key: "1Key0", expiresAtMs: null }]);
  });

  it("prunes expired held keys and reports next expiry", () => {
    const entries = [
      { key: "1Key0", expiresAtMs: 1000 },
      { key: "1Key1", expiresAtMs: 3000 },
      { key: "1Key2", expiresAtMs: null },
    ];

    expect(prunePreviewActiveKeys(entries, 2000)).toEqual([
      { key: "1Key1", expiresAtMs: 3000 },
      { key: "1Key2", expiresAtMs: null },
    ]);
    expect(getNextPreviewExpiryMs(entries)).toBe(1000);
    expect(getNextPreviewExpiryMs([{ key: "a", expiresAtMs: null }])).toBeNull();
  });
});
```

- [ ] **Step 2: 确认失败** — `npm run test -- previewActiveKeys`。

- [ ] **Step 3: 实现** `src/lib/previewActiveKeys.ts`

```ts
import type { Note } from "../types/score";

export type PreviewActiveKeyEntry = {
  expiresAtMs: number | null;
  key: string;
};

export function applyNoteGroupToPreviewActiveKeys(
  currentEntries: PreviewActiveKeyEntry[],
  notes: Note[],
  nowMs: number,
  playbackSpeed: number,
): PreviewActiveKeyEntry[] {
  const groupKeys = new Set(notes.map((note) => note.key));
  const survivingHolds = currentEntries.filter(
    (entry) =>
      entry.expiresAtMs !== null &&
      entry.expiresAtMs > nowMs &&
      !groupKeys.has(entry.key),
  );
  const newEntries = notes.map((note) => ({
    expiresAtMs:
      note.duration === undefined
        ? null
        : nowMs + note.duration / playbackSpeed,
    key: note.key,
  }));

  return [...survivingHolds, ...dedupeByKeyKeepingLatestExpiry(newEntries)];
}

export function prunePreviewActiveKeys(
  currentEntries: PreviewActiveKeyEntry[],
  nowMs: number,
): PreviewActiveKeyEntry[] {
  return currentEntries.filter(
    (entry) => entry.expiresAtMs === null || entry.expiresAtMs > nowMs,
  );
}

export function getNextPreviewExpiryMs(
  currentEntries: PreviewActiveKeyEntry[],
): number | null {
  const expiries = currentEntries
    .map((entry) => entry.expiresAtMs)
    .filter((expiry): expiry is number => expiry !== null);

  return expiries.length === 0 ? null : Math.min(...expiries);
}

function dedupeByKeyKeepingLatestExpiry(
  entries: PreviewActiveKeyEntry[],
): PreviewActiveKeyEntry[] {
  const deduped = new Map<string, PreviewActiveKeyEntry>();

  for (const entry of entries) {
    const existing = deduped.get(entry.key);

    if (
      !existing ||
      existing.expiresAtMs === null ||
      (entry.expiresAtMs !== null && entry.expiresAtMs > existing.expiresAtMs)
    ) {
      deduped.set(entry.key, entry);
    }
  }

  return Array.from(deduped.values());
}
```

(同组同键 tap+hold 并存时保留 hold?注意 `dedupeByKeyKeepingLatestExpiry` 当 existing 为 tap(null)时会被任何新条目覆盖——包括另一个 tap,结果仍是 tap,正确;hold 覆盖 tap,正确;测试若发现语义出入以测试为准修正。)

`usePreviewPlayback.ts` 接线:

- 新增 `const activeKeyEntriesRef = useRef<PreviewActiveKeyEntry[]>([]);` 与 `const activeKeyPruneTimerRef = useRef<number | null>(null);`
- note group 回调(约 185 行)改为:

```ts
(noteGroup) => {
  const nowMs = performance.now();
  activeKeyEntriesRef.current = applyNoteGroupToPreviewActiveKeys(
    activeKeyEntriesRef.current,
    noteGroup,
    nowMs,
    playbackSpeedRef.current,
  );
  setActiveKeys(activeKeyEntriesRef.current.map((entry) => entry.key));
  scheduleActiveKeyPrune();
  appendLog(
    formatText(text.logs.playingPreviewKey, {
      key: noteGroup.map((note) => note.key).join(", "),
    }),
  );
},
```

- 新增局部函数(hook 内):

```ts
function clearActiveKeyPruneTimer() {
  if (activeKeyPruneTimerRef.current !== null) {
    window.clearTimeout(activeKeyPruneTimerRef.current);
    activeKeyPruneTimerRef.current = null;
  }
}

function scheduleActiveKeyPrune() {
  clearActiveKeyPruneTimer();
  const nextExpiryMs = getNextPreviewExpiryMs(activeKeyEntriesRef.current);

  if (nextExpiryMs === null) {
    return;
  }

  const delayMs = Math.max(0, nextExpiryMs - performance.now());
  activeKeyPruneTimerRef.current = window.setTimeout(() => {
    activeKeyPruneTimerRef.current = null;
    activeKeyEntriesRef.current = prunePreviewActiveKeys(
      activeKeyEntriesRef.current,
      performance.now(),
    );
    setActiveKeys(activeKeyEntriesRef.current.map((entry) => entry.key));
    scheduleActiveKeyPrune();
  }, delayMs);
}

function resetActiveKeys() {
  clearActiveKeyPruneTimer();
  activeKeyEntriesRef.current = [];
  setActiveKeys([]);
}
```

- 所有现有 `setActiveKeys([])` 调用点(stopCurrentPreview、onFinish、handlePausePreview)替换为 `resetActiveKeys()`。

- [ ] **Step 4: 确认通过** — `npm run test` + `npx tsc --noEmit`。

- [ ] **Step 5: 提交** — `git commit -m "Hold preview keyboard highlights for sustained notes"`。

### Stage 4 收尾

- [ ] 全量门禁四连。
- [ ] 冒烟:`npm run tauri dev`,预览播放 v1 曲(高亮行为与之前一致)。

---

## Stage 5:工具链与文档

### Task 10: 内置索引生成器 v2 一致性

**Files:**
- Modify: `scripts/generate-builtin-score-index.mjs`(`isValidObjectNotes`、`getDurationMs`、`createEntry`)
- Test: `src/lib/builtinScoreIndex.test.ts` 或 `builtinScoreLoader.test.ts`(先读两个文件,把 v2 用例加到已承担"生成器/运行时一致性"职责的那个;若无此职责的现有测试,加到 `builtinScoreLoader.test.ts`)

**Interfaces:**
- 生成器规则与运行时解析一致:`formatVersion` 缺省/1/2 之外 → 跳过该歌曲;v2 歌曲音符 `duration` 违反 `0 < d <= 60000` → 跳过该歌曲;`durationMs` = 原分组累计值 + 源时间尾巴(与 `getSustainTailMs` 同公式)。

- [ ] **Step 1: 写失败测试** — 用与生成器相同的输入构造 v2 歌曲字符串,断言 `parseScoreFileSongAtIndex` 接受并保留 duration(运行时侧);生成器侧逻辑是 .mjs 无法直接被 Vitest 引用——测试镜像其规则:构造同一首 v2 歌,断言运行时解析结果的 `getSustainTailMs` 值等于测试内按生成器公式手算的尾巴(保证两套公式由同一测试钉住)。

```ts
it("keeps v2 lazy-loaded durations consistent with generator duration formula", () => {
  const raw = JSON.stringify([
    {
      name: "V2 Builtin",
      bpm: 120,
      bitsPerPage: 16,
      pitchLevel: 0,
      isComposed: true,
      formatVersion: 2,
      songNotes: [
        { time: 0, key: "1Key0", duration: 5000 },
        { time: 1000, key: "1Key1" },
      ],
    },
  ]);
  const song = parseScoreFileSongAtIndex(raw, 0);

  expect(song?.songNotes[0].duration).toBe(5000);
  // 生成器公式:分组增量和(=1000) + 尾巴(5000-1000=4000) = 5000
  expect(1000 + getSustainTailMs(song?.songNotes ?? [])).toBe(5000);
});
```

- [ ] **Step 2: 确认失败/通过基线** — `npm run test -- builtinScore`(运行时侧可能已通过——Task 1 已支持;重点是生成器改动后索引再生成不炸)。

- [ ] **Step 3: 实现生成器**

```js
function readFormatVersion(song) {
  if (song.formatVersion === undefined) {
    return 1;
  }

  return song.formatVersion === 1 || song.formatVersion === 2
    ? song.formatVersion
    : null;
}

function isValidObjectNotes(songNotes, formatVersion) {
  return (
    Array.isArray(songNotes) &&
    songNotes.every((note) => {
      if (typeof note !== "object" || note === null || Array.isArray(note)) {
        return false;
      }

      if (
        readFlexibleNumber(note.time, null) === null ||
        typeof note.key !== "string"
      ) {
        return false;
      }

      if (formatVersion === 2 && note.duration !== undefined) {
        return (
          typeof note.duration === "number" &&
          Number.isFinite(note.duration) &&
          note.duration > 0 &&
          note.duration <= 60000
        );
      }

      return true;
    })
  );
}

function getSustainTailMs(songNotes, formatVersion) {
  if (formatVersion !== 2) {
    return 0;
  }

  const lastGroupTime = songNotes.reduce(
    (lastTime, note) => Math.max(lastTime, readFlexibleNumber(note.time, 0)),
    0,
  );
  const maxEnd = songNotes.reduce((max, note) => {
    if (typeof note.duration !== "number") {
      return max;
    }

    return Math.max(max, readFlexibleNumber(note.time, 0) + note.duration);
  }, 0);

  return Math.max(0, maxEnd - lastGroupTime);
}
```

`createEntry`:开头读 `const formatVersion = readFormatVersion(song); if (formatVersion === null) return null;`;`isValidObjectNotes(song.songNotes, formatVersion)`;`durationMs: getDurationMs(song.songNotes) + getSustainTailMs(song.songNotes, formatVersion)`。

- [ ] **Step 4: 再生成索引并回归** — `npm run generate:builtin-index`,然后 `git diff public/builtin-scores/index.json` 应只有 `generatedAt` 变化(1.1 万 v1 谱输出不变——这是生成器改动的关键回归证据);`npm run test` 全绿。若 index.json 仅 `generatedAt` 变化,还原该文件(`git checkout -- public/builtin-scores/index.json`)避免无意义 diff。

- [ ] **Step 5: 提交** — `git commit -m "Support scores-v2 in builtin score index generator"`。

### Task 11: 格式文档 + 示例谱

**Files:**
- Create: `docs/scores-v2-format.md`(面向 AI/转换器作者的严格规范:文件结构、字段表、约束、完整示例、常见错误对照 — 只写规范写法,绝对毫秒,不提解析器宽松容错)
- Create: `docs/examples/scores-v2-sample.txt`(合法 v2 文件:一首含长音+和弦+混 v1 音符的曲,一首纯 v1 曲,演示混装)

**Interfaces:** 无代码。文档字段约束必须逐字对齐 Task 1 实现(`formatVersion` 数字 1|2、`duration` (0, 60000] 毫秒、v1 中 duration 无效)。

- [ ] **Step 1: 写文档与示例**(示例文件先用 `parseScoreFileContent` 的测试临时验证可解析,或直接在 dev 中导入验证)
- [ ] **Step 2: 提交** — `git commit -m "Add scores-v2 format documentation and sample"`。

### Task 12: 终验

- [ ] **Step 1: 全量门禁** — `npm run test -- --testTimeout=30000` / `npx tsc --noEmit` / `npm run build` / `cd src-tauri && cargo check && cargo test`。
- [ ] **Step 2: 冒烟(dev)** — `npm run tauri dev`:导入 `docs/examples/scores-v2-sample.txt` → 曲库显示时长含尾音 → 预览播放长音键持续高亮 → (有条件时)后台播放对延音乐器实测。v1 内置谱抽查播放无回归。
- [ ] **Step 3: 检查 `src-tauri/Cargo.toml`** 无行尾噪声;`git status` 干净。
- [ ] **Step 4: 汇报** — 变更总结 + 建议的合并命令(merge 回 main 由用户执行):

```bash
git checkout main
git merge --no-ff feature/scores-v2
git push origin main
git branch -d feature/scores-v2
```

## Self-Review 记录

- 规格覆盖:设计文档 §1→Task 1/2,§2→Task 3/4/5,§3→Task 6,§4→Task 7,§5→Task 8/9,§6→Task 10,格式文档/示例→Task 11,验证→Task 12。无缺口。
- 占位符:Task 4 Step 1 与 Task 8 Step 1 的个别用例依赖现有测试 helper,已注明"先读文件按最接近用例补全断言",属实现时定形,非 TBD。
- 类型一致性:`MappedPlaybackKey`/`PlannedPlaybackKey`(TS)与 `PlannedKey`(Rust, serde camelCase `holdMs`)命名已对齐;`getSustainTailMs` 在 Task 3 定义、Task 8/10 复用,签名一致。
- 设计偏差备注:设计文档 §4 的 total_ms 公式原为"逐组结束取最大",计划统一简化为"最后组起点 + 源时间尾巴/speed"(四处一致、更简单);已在计划 Architecture 段落声明,实现以计划为准。
